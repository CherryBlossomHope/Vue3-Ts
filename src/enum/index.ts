// 用户权限
export enum userPermission {
    '超级管理员',
    '天天娘娘,万福金安'
}

// 本地存储
//字典 Dictionaries    expire过期时间key    permanent永久不过期
export enum Dictionaries {
    expire = '__expire__',
    permanent = 'permanent'
}